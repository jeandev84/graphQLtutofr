enum TodoStatusEnum {
   WAITING
   IN_PROGRESS
   CANCLED
   DONE
}


type todo {
   id: ID!
   name: String!
   content: String!
   status: TodoStatusEnum!
}

type Query {
     hello(name: String): String!
}
